{
  "name": "talos",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "testing-protocol",
    "control-plane",
    "worker"
  ],
  "scripts": {
    "build": "tsc -b",
    "build:testing-protocol-runtime": "npm run build --workspace @talos/testing-protocol && npm run build:runtime --workspace @talos/testing-protocol",
    "build:control-plane-runtime": "npm run build:testing-protocol-runtime && npm run build:runtime --workspace @talos/control-plane",
    "build:worker-runtime": "npm run build:testing-protocol-runtime && npm run build:runtime --workspace @talos/worker",
    "typecheck": "tsc -b --pretty false",
    "lint": "eslint \"**/*.{ts,tsx}\"",
    "bundle:worker": "node scripts/bundle-worker.mjs",
    "generate:testing-fixtures": "npm run build && node scripts/generate-testing-contract-fixtures.mjs",
    "verify:testing-protocol-runtime": "node scripts/verify-runtime-distribution.mjs testing-protocol testing-protocol/dist-runtime testing-protocol/src testing-protocol/package.json",
    "verify:control-plane-runtime": "node scripts/verify-runtime-distribution.mjs control-plane control-plane/dist-runtime control-plane/src control-plane/package.json",
    "verify:worker-runtime": "node scripts/verify-runtime-distribution.mjs worker worker/dist-runtime worker/src worker/package.json",
    "verify:runtime-distributions": "npm run verify:testing-protocol-runtime && npm run verify:control-plane-runtime && npm run verify:worker-runtime",
    "demo:testing-contract": "tsc -b testing-protocol --force --pretty false && node scripts/demo-testing-contract.mjs",
    "test": "vitest run --coverage",
    "test:mongo-contract": "vitest run control-plane/src/storage/repository-contract.test.ts",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "@vitest/coverage-v8": "^2.1.8",
    "esbuild": "^0.28.2",
    "eslint": "^9.17.0",
    "mongodb-memory-server": "^11.2.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
